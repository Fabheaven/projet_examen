{% extends 'base.html.twig' %}

{% block body %}
<div class="container mx-auto">
   <h1 class="text-5xl font-bold text-center mt-24 mb-8">Nos différentes circuits</h1>
</div>

<!-- Conteneur des cartes -->
<div class="container flex flex-wrap justify-center gap-8 mt-8">
    {% for circuit in circuits %}
        {% include "components/circuitcard.html.twig" with { circuit: circuit } only %}
    {% endfor %}
</div>
{% endblock %}